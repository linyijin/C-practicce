#include "vector.h"
#include<iostream>
//using namespace VECTOR;
/*
int main()
{
	VECTOR::Vector vector(20, 30, VECTOR::Vector::Mode(0));//直接用枚举代表的整数也可以声明类型
	VECTOR::Vector shove = VECTOR::Vector(20, 30);//间接使用构造函数创建对象
	VECTOR::Vector diff = vector - shove;//根据形式选择重载的运算符  vector.operator-(shove);
	std::cout << diff << std::endl;
	std::cout <<  - shove << std::endl;
	system("pause");

}*/